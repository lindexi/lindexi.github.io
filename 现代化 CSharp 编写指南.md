# 现代化 C# 编写指南

这是给 AI 看的提示词内容

<!--more-->
<!-- CreateTime:2026/03/04 07:18:40 -->

<!-- 不发布 -->

## General Guidelines

- 网络层统一使用 `HttpClient` + `HttpRequestMessage`，避免 `HttpWebRequest`。
- JSON 处理优先使用 `System.Text.Json`，并尽量配合源生成上下文（`JsonSerializerContext`）提升 AOT 兼容性。
  - 将所有 `Newtonsoft.Json` 的功能替换为调用 `System.Text.Json` 实现
  - 禁止通过伪造兼容层保留 `Newtonsoft` 特性；应直接迁移为 `System.Text.Json` 特性与 API
- 对关键入参增加早期校验（如 `ArgumentNullException.ThrowIfNull` / `ArgumentException.ThrowIfNullOrWhiteSpace`），减少隐式空引用风险。
-. 常用集合初始化时尽量预估容量（如 `new List<T>(count)`、`new Dictionary<TKey, TValue>(capacity)`），减少扩容开销。
- 字符串拼接优先使用内插字符串（`$"..."`），替代 `string.Format`，可读性更高。
- 属性、方法、字段、类型名等的命名需要符合 C# 命名规范，例如：
  - 属性、方法、类型名应该使用 PascalCasing 命名风格
  - 局部变量应该使用 camelCase 命名风格
  - 字段应该使用 `_camelCase` 风格，而且尽可能使用 `readonly ` 标记只读。对于 `static readonly` 来说，应该视为常量，应该采用 PascalCasing 命名风格
- 最好不要公开字段，请封装为属性进行公开
- 考虑可空。但可以对进行网络请求的纯数据结构放宽要求
- 改造时，考虑 API 兼容性。旧 API 的命名保留兼容入口（`[Obsolete]`），新增规范命名 API 并逐步迁移

- 路径拼接应使用 `Path.Join` 方法，而非 `Path.Combine`
- 对于 UI 框架相关代码，需先理解整体代码实现逻辑，总结 MVVM 最佳实践，再按实践进行框架设计。界面代码与逻辑代码须严格分离，做到数据驱动。界面美化应抽取为独立的资源文件，对所有涉及的控件统一定义样式，并做好整体主题色调处理，同时确保设计时（Design-time）的良好支持

- 单元测试应该采用 `[TestMethod(DisplayName = "xxx")]` 的格式，用 DisplayName 标记当前单元测试的名字，且尽量采用中文名，除非原本项目里面就是采用英文名的单元测试


### 代码质量

- 所有公共 API 必须有 XML 文档注释
- Nullable reference types 启用，代码必须 null-safe
- 异步方法使用 `ConfigureAwait(false)`
- 优先使用 C# 最新语言特性（记录类型、模式匹配等）
- 遵循 KISS 原则，代码应简洁明了，避免过度设计
- 注释应说明"为什么"而非"是什么"，文件名和代码结构本身即文档
- 避免在代码中引用临时方案名称（如"方案A"、"Plan B"），这些会失去上下文
- 不保留仅包含注释的空文件，每个文件都应有实际代码价值

### 对于编写文档工作

- 需要先阅读原本的文档，总结原文档编写风格，按照原风格编写新的文档
- 文档编写完成之后，需要重新审查整篇文档，以及其相关文档内容
  - 尽量调用子智能体进行审查文档
  
### 方案设计的约束

#### 1. 控制复杂度，优先稳定性

- 设计方案不应引入过多的层次、状态、注册中心、协调器、生命周期阶段、租约、回退路径、功能开关和回滚策略。
- 如果设计实现出来会充满补丁式分支和防御性回退逻辑，说明设计本身需要简化。
- 以**可证明正确性**（而非"看起来覆盖了所有场景"）作为衡量标准。复杂度的增加必须带来等比例的确定性，否则应拒绝。

#### 2. 用抽象而非枚举来证明完备性

- 不应以枚举具体实现来代替抽象。
- 枚举增长时，暴露的不是"覆盖全面"，而是"设计仍由具体场景和实现细节驱动"。
- 正确的抽象应当让新场景被**推导**出来，而非被**添加**进去。如果每次新增场景都需要修改核心模型，说明抽象点找错了。

#### 3. "首期不做"不等于"首期已做到"

- 把未完成、未验证或无法统一解释的部分推迟为"首期不做""PoC 阶段""后续扩展"，这本身不构成设计。
- 首期结构必须具备**可证的自然演进能力**——即必须先证明首期的结构能够平滑承载、自然演进到后续目标，而不是后续一来就要推倒重来。
- 否则只有首期；首期之后就是重做。

#### 4. 文档信息密度要高，避免重复

- 相同的知识点不应在多处、多份文档中重复出现。
- 重要结论不应被大量枚举、模板、阶段说明和防御性描述所淹没。
- 多份文档不应重复描述相同的背景、状态、风险、流程和边界。
- 判断标准：篇幅增长是否带来了等比例的信息确定性？如果篇幅翻倍而确定性不变，应压缩和去重。

### 禁止事项

- 禁止在代码或注释或文档等地方暴露我个人的敏感信息，包含但不限于：
  - 我的密码或 API Key 信息（明确硬编码的内容除外，或现有代码非本次引入内容除外）
  - 我的本地磁盘绝对路径
  - 我的内部 gitlab 路径（但允许 github.com 的路径）
- 禁止通过脚本写入文件内容。应该调用工具写入文件内容

注： 如有调用子智能体，请将 Guidelines （特别是禁止事项）明确告知子智能体

---

现在这个界面不美观，很多控件都是采用原生的样式，需要你进行美化一下。然后整个逻辑代码现在都写在    里面，这不符合 MVVM 规范。请理解整体代码实现逻辑，先总结 MVVM 最佳实践，然后按照实践进行框架设计，再按照你的设计重构代码，要求界面代码和逻辑代码分离，做好数据驱动。界面美化应该抽独立的资源文件，对所有涉及的控件都定义样式，且做好整体主题色调处理

---

这是一个 xxx 项目，请先搭建完成项目结构，需要包含用于记录文档的文件夹，文件夹里面分为过程文件记录和经验文档记录，要求每完成一件事情，都向文档进行记录。第一步是回忆和总结 WPF 的 MVVM 最佳实践，输出经验文档。第二步是进行需求分析，需求是 xxxxxxxx。第三步是进行项目框架设计。第四步是进行编码实现。第五步是进行代码审查和文档编写和整理

---

更改之前，请先回忆和总结C#最新语法的写法，沉淀到经验文档

---

你是一个监督者的角色，你应该按照任务计划，将任务分派给子智能体去执行，调用 run_subagent 工具，让子智能体去编写代码，去探索项目情况。你需要明确告知子智能体，禁止调用命令行终端和执行脚本。你自己不要去编写任何代码，也不要去进行任何的探索，你只是用于监督各个子智能体，确保任务能够全部地完成

---

# 通用C#基础库/辅助类开发规范（AI生成通用提示词）
## 文档用途
用于指导AI生成**生产级、无业务耦合、可复用的C#基础类库/辅助类**，适用于任何业务场景（文件处理、音视频操作、网络请求、工具封装等），要求代码符合.NET生态规范、健壮性强、易维护、无耦合。

## 核心设计原则（所有场景通用）
1.  **实例优先原则**：优先实现为非静态实例类，仅无状态纯工具可使用静态类；禁止用静态类承载有状态逻辑
2.  **强类型优先原则**：所有入参/出参禁止用无意义的字符串、字典等弱类型传递，必须自定义强类型记录/类承载业务参数
3.  **可配置优先原则**：禁止硬编码配置项，所有可变参数支持上层传入，提供合理默认值
4.  **无副作用原则**：类库内部产生的临时资源、状态必须自动清理，无需上层调用者手动处理
5.  **无耦合原则**：禁止耦合特定业务逻辑、特定输出端（比如控制台、特定日志框架），支持上层自定义扩展

## 强制编码规范（所有场景必须遵守，禁止违反）
| 规则分类 | 要求 | 禁止行为 |
|----------|------|----------|
| 路径与IO处理 | 1. 所有文件/目录路径必须用`FileInfo`/`DirectoryInfo`强类型传递<br>2. 路径拼接必须使用`Path.Join`方法<br>3. 临时文件/目录名必须用`Path.GetRandomFileName`生成 | 1. 禁止用字符串传递路径参数<br>2. 禁止使用`Path.Combine`拼接路径<br>3. 禁止用Guid生成临时文件名<br>4. 禁止硬编码任何固定路径 |
| 异步与并发 | 1. 所有IO、进程、网络等耗时操作必须实现为全异步方法，方法名带`Async`后缀<br>2. 等待进程/IO/任务必须使用异步等待方法（比如`WaitForExitAsync`、`await`）<br>3. 所有异步方法必须支持`CancellationToken`取消令牌 | 1. 禁止用同步等待（比如`WaitForExit()`、`.Result`、`.Wait()`）阻塞线程<br>2. 禁止异步方法不支持取消操作 |
| 资源管理 | 1. 持有非托管资源、临时资源的类必须实现`IAsyncDisposable`接口<br>2. 类库内部产生的临时文件、连接、句柄必须自动清理，异常场景也不例外 | 1. 禁止让上层调用者手动清理类库内部产生的临时资源<br>2. 禁止资源泄漏 |
| 可观测性 | 1. 所有日志、错误信息必须通过通用回调/标准`ILogger`抽象透出<br>2. 异常必须保留完整堆栈，禁止吞异常，可包装后抛出 | 1. 禁止直接向控制台、文件等特定输出端写日志<br>2. 禁止隐藏错误信息，返回模糊的成功/失败状态 |
| 参数校验 | 所有公共方法入参必须做前置合法性校验，提前拦截无效输入 | 禁止不校验参数直接执行逻辑，导致内部抛出模糊异常 |

## API设计通用要求
1.  **无歧义入参**：禁止用`Dictionary`、`object`、字符串等弱类型承载结构化参数，必须自定义`record`/`class`承载业务参数，参数名要语义化
2.  **依赖注入友好**：依赖项（比如外部工具路径、配置、日志）优先通过构造函数传入，禁止在类内部硬初始化依赖
3.  **兼容性**：默认兼容.NET 6+ LTS版本，特殊版本要求需明确声明
4.  **可扩展性**：核心逻辑支持上层自定义扩展，禁止写死不可修改的逻辑

## 输出要求
1.  所有公共类型、方法必须包含完整的XML注释，说明用途、参数含义、返回值、异常情况
2.  必须包含上层调用示例代码，示例可直接运行
3.  代码必须符合.NET官方编码规范，命名遵循PascalCase/CamelCase标准
4.  异常要分类处理，业务异常和系统异常要明确区分
5.  无需业务场景特定的硬编码逻辑，可直接复用在不同项目中


## 更新规则

- 只记录已经验证过的事实；未验证内容必须明确写成“待确认”或“不确定”。
- 若新的构建结果与旧记录冲突，应直接改写旧结论，不要把互相矛盾的描述同时保留。
- 优先记录以下信息：
  - 当前解决方案实际纳入的项目
  - 当前磁盘上已存在但尚未纳管的项目
  - 最新构建入口、构建命令和首个真实失败点
- 每次推进后都要更新“当前状态”“阻塞点”“下一步建议”。
- 文档中不要写“本轮”“本次”之类依赖上下文轮次的描述，应直接写当前状态和后续动作。
- 工作过程中发现的问题若不属于当前优先任务范围，应记录到 `backlog.md`，不要打断当前迁移顺序。


---

# 避免无效抽象

抽象的目的，是降低理解和维护代码的成本。不能仅因为一段代码可以被提取，就创建方法、常量、工厂或类型。

新增抽象前，应确认它至少能够做到一项：

- 集中维护多处相同的不变量；
- 隐藏调用方不需要了解的复杂实现；
- 管理真实存在的状态或生命周期；
- 提供明确的复用价值；
- 让业务意图比直接代码更清楚。

以下情况通常不应抽象：

- 只调用另一个方法的单行转发方法；
- 只使用一次且在原位置含义明确的常量；
- 没有状态，却要求调用方先创建实例再调用的 API；
- 只包装简单对象初始化、但不维护任何约束的工厂方法；
- 参数含义不同，却因为代码形式相似而被强行合并的操作。

判断标准不是代码是否更短、结构是否更多，而是读者能否更直接地理解代码。

修改完成后，应从调用处重新阅读：

1. 调用是否直接表达业务动作；
2. 是否暴露了无关的创建或配置过程；
3. 每个新增抽象是否确实减少了复杂度；
4. 删除该抽象后，代码是否反而更容易理解。

如果抽象没有明确收益，就不要添加。

---

修改代码时：

- 先理解现有代码和外部契约，不猜测参数、格式或行为。
- 优先写直接、容易读懂的代码。
- 不添加没有实际作用的工厂方法、转发方法或包装层。
- 不为只使用一次且含义明确的值声明常量。
- 相同的不变量应由真正负责它的类型统一维护。
- 形式相似但契约不同的操作不要强行合并。
- 不向调用方暴露其不需要关心的对象创建细节。
- 构建通过后重新阅读整个修改范围，检查是否存在多余结构和职责错位。

---

### 文档/注释撰写守则

1. 撰写时注意目标读者
    - 代码注释的目标读者为代码维护人员
    - 文档的目标读者为代码维护人员，或产物用户
    - 不存在目标读者为智能体会话参与人的代码、注释或文档，而这也是 AI 撰写文档时最容易犯的错误
        - 如提到仅智能体会话上下文才存在的引用：`方案 A`、任务编号 `T8` `Q3`、章节编号 `§x.x`
        - 如使用智能体对话语气：`按你说的`、`给用户看的`
    - 文档和注释尽量使用正向描述（是什么，应该怎么），而非负向描述（不是什么，不应该怎么），因为 AI 写文档时的负向描述大多都是对凭空出现的离谱概念进行反驳，不具备参考价值
2. 文档/注释的撰写应遵循奥卡姆剃刀原则
    - 只写必须要写的内容，禁止长篇大论
    - 避免在各处澄清同一个描述，这会让此描述几乎无法修改和维护，应尽量集中在一处说明（在一些地方不提及这些描述看似存在歧义风险，实则整体文档/注释/代码已经具备确定性解释，同时还更利于修改和维护）
    - 避免注释间交叉引用和相互说明，这会导致注释难以修改；如需描述其他模块与自身的交互，尽量使其通用化或抽象化，避免特指某一个或某几个模块
    - 除导航地图类文档外，避免文档间相互引用（尤其避免网状文档引用），因为 AI 编写的文档经常强调无关紧要的细节导致很容易过时，进而导致随着引用网传递使所有文档过时

